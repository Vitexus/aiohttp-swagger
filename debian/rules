#!/usr/bin/make -f

export DH_VERBOSE = 1
export PYBUILD_NAME = aiohttp-swagger

%:
	dh $@ --with python3 --buildsystem=pybuild

override_dh_auto_test:
	# Skip tests during build to avoid dependency issues
